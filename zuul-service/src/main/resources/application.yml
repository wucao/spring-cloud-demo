eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8769

spring:
  application:
    name: zuul-service

zuul:
  routes:
    hello-service:
      path: /hello-service/**
      serviceId: hello-service